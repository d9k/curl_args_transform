{
  "tasks": {
    "install": "deno install --force --allow-read --name curl_args_transform main.ts",
    "test": "deno test",
    "run": "deno run --allow-read main.ts",
    "debug": "deno run --unstable --inspect --allow-all main.ts"
  },
  "fmt": {
    "indentWidth": 2,
    "lineWidth": 60,
    "singleQuote": true
  }
}
